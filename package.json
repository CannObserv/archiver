{
  "name": "archiver-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "npx eslint src/dashboard/static tests/js",
    "lint:js": "npm run lint",
    "format:js:check": "echo 'No JS formatter configured (Prettier not adopted)' && exit 0",
    "test:js": "npm run test"
  },
  "devDependencies": {
    "@eslint/js": "latest",
    "happy-dom": "latest",
    "vitest": "latest"
  }
}
