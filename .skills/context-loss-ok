# Warranted losses (curating-context Phase 6). One judged line each.
#
# The Alpine catalogue index moved from docs/STYLE.md to docs/COMPONENTS.md
# (archiver#178 curation). Every row ended "Full docs in `docs/COMPONENTS.md`."
# - a pointer into the very file the row now lives in. This change is what
# moved the target, so the row arrives with the pointer dropped.
docs/STYLE.md :: retarget :: `sortableChips` | `main.js` | Chip strip for selector/rep-field suggestions
docs/STYLE.md :: retarget :: `repFieldsEditor` | `main.js` | Wrapper for the rep_fields textarea
docs/STYLE.md :: retarget :: `domainNotes` | `main.js` | Edit/view toggle for the notes row
docs/STYLE.md :: retarget :: `registerWizard` | `main.js` | 4-step registration wizard state
docs/STYLE.md :: retarget :: `previewNameDispatch` | `main.js` | One-shot dispatcher: bubbles a `preview-name`
docs/STYLE.md :: retarget :: `urlCheckDispatch` | `main.js` | One-shot dispatcher (#53)

# archiver#181 curation: `## Detail Screen Conventions` was 56% of docs/UI.md,
# so it split out to docs/SCREENS.md. Three lines described UI.md as the
# file holding those conventions and one counted its siblings; this change is
# what moved the target, so each is rewritten in place rather than relocated.
# The section body itself moved verbatim - 200 lines, 0 duplicated.
docs/UI.md :: retarget :: patterns, and the conventions every detail screen follows.
docs/UI.md :: retarget :: The other two parts of this reference, under the same living-doc rule:
docs/UI.md :: retarget :: **The dashboard's shared mechanics: URL map, proxy-header auth, HTMX swap

# archiver#181 CR round 2, finding 9: three PAGES.md detail entries re-derived
# the canonical `.entity-card` header anatomy - eyebrow, the `<h1
# class="entity-card__title" id="…" tabindex="-1">`, the focus-target reason -
# once per screen. That anatomy acquired a canonical home in this same change
# (docs/SCREENS.md § Header), so each entry now points there and keeps only what
# is true of its own screen: the eyebrow text, the title value, the heading id,
# and the issue that adopted it. Nothing screen-specific was dropped.
docs/PAGES.md :: retarget :: `.eyebrow` "Domain" kicker
docs/PAGES.md :: retarget :: `.eyebrow` "Information Source" → `<h1 class="entity-card__title" id="info-source-heading"
docs/PAGES.md :: retarget :: `.eyebrow` "Replication Specification" → `<h1 class="entity-card__title" id="rep-spec-heading"

# archiver#182 curation: four HTMX/Alpine pattern sections left docs/STYLE.md for
# docs/UI.md, whose stated scope is the dashboard's shared mechanics - none of
# the four contained any CSS. Three moved whole. The fourth was a near-duplicate
# of UI.md's own "Inline validation errors (`hx-target-422`)": UI.md had the
# prose, STYLE.md the worked example and the rules. The body merged into that
# canonical section rather than landing beside it as a second copy, so only the
# now-redundant heading has no home.
docs/STYLE.md :: rename :: ### HTMX inline form error pattern

# Same change: the pointer named docs/STYLE.md for a section this run moved to
# docs/UI.md, so it is retargeted in place rather than relocated.
docs/COMPONENTS.md :: retarget :: the reason it exists is `docs/STYLE.md` § **JSON data island pattern**

# The Error-pages body moved from docs/PAGES.md to docs/UI.md (archiver#112 CR
# round 2, finding 6). Its closing pointer named UI.md as an external doc; inside
# UI.md that self-link read as pointing elsewhere, so it was retargeted to the
# intra-doc section reference.
docs/UI.md :: retarget :: [UI.md](UI.md) § Failures are surfaced, not swallowed.

# archiver#147 CR finding 7. The Conventions section carried four separate
# trailing pointers at docs/CONVENTIONS.md; they are one pointer at the section
# head now, which names every target the four named between them (regex scope,
# PLC0415 exemptions, the journald contract, the error envelope's kind
# vocabulary). `duplicate` is the warrant: the surviving head pointer carries
# the destination and every target the four named, so the content is elsewhere
# in the surface rather than dropped.

# 2026-09-09 AGENTS.md curation (6831 -> ~6280 exact tokens). Every entry below
# is a class-C tightening: the section was rewritten in place with the same
# claims in fewer words, so whole-line matching reports it as lost while atom
# matching (--claims) confirms the claims carried across - 169 atoms at base,
# 1 dropped and separately warranted in .skills/context-claims-ok.
#
# Nothing here was demoted: the destinations (docs/SCHEMA.md, docs/SKILLS.md,
# docs/DEPLOYMENT.md, docs/CONVENTIONS.md, docs/reference/tailscale.md) already
# documented this material in more depth than AGENTS.md summarised it, so a
# verbatim move would have appended near-duplicates beside better text.
AGENTS.md :: tighten :: Central registry + authoring service for the Cannabis Observer information layer. FastAPI + PostgreSQL. Owns five registry tables (`info_items`, `info_sources`,
AGENTS.md :: tighten :: Python ≥3.12, uv, pytest, ruff. **Postgres 16 on archiver's own VM** - a dedicated instance since #193 D5, not the one shared with watcher and notifier. Three d
AGENTS.md :: tighten :: `redis://default:<password>@broker:6379/0` - 1 ms direct, same region. This
AGENTS.md :: tighten :: verification - must target 8001, never 8000.
AGENTS.md :: tighten :: - Docstrings for public modules, classes, functions
AGENTS.md :: tighten :: - Explicit imports only

# 2026-09-09 AGENTS.md curation, ROUND 2 (6270 -> 5999 exact tokens; under the
# 6000 budget round 1 could not reach). Class C again, plus three demotions
# whose destination already owned the rule: Dashboard living docs and the
# ExecStartPre journald contract to docs/CONVENTIONS.md, the skills-directory
# facts to docs/SKILLS.md. Atom matching confirms the claims carried: 0 dropped.
#
# Round 1 concluded the remainder was class A and stopped 270 over. That was
# tested rather than trusted here - the ledger showed the file measured 5916 on
# 2026-09-01, so the budget was demonstrably reachable, and the gap turned out
# to be prose in sections round 1 had classified whole instead of by subsection.
AGENTS.md :: tighten :: Be terse. Prefer fragments over full sentences. Skip filler and preamble. Sacrifice grammar for density. Lead with the answer or action.
AGENTS.md :: tighten :: TDD required. Red → Green → Refactor. No production code without a failing test first.
AGENTS.md :: tighten :: SocratiCode is configured here (`.socraticodecontextartifacts.json` present); the index itself is per-host, and the SessionStart health hook reports whether it 
AGENTS.md :: tighten :: **The broker is not operated from this repo (#193 D6).** Its tuning, its
AGENTS.md :: tighten :: health probe, and the cluster stream inventory live in
AGENTS.md :: tighten :: [CannObserv/broker](https://github.com/CannObserv/broker); archiver is a
AGENTS.md :: tighten :: client. One seam survives that split and has no test spanning it: the broker
AGENTS.md :: tighten :: config's `maxmemory` cap and `OutOfMemoryError` being transient in
AGENTS.md :: tighten :: dashboard is `https://co-registrar.exe.xyz/`, the dev server
AGENTS.md :: tighten :: the broker is on a third node (CannObserv/broker#1).
AGENTS.md :: tighten :: **The broker is now a network hop.** Archiver reaches it over the tailnet as
AGENTS.md :: tighten :: answers to two names, `co-registrar` and `archiver`, both on port 8000, so **an
AGENTS.md :: tighten :: HTTP 200 on a short name proves nothing about the tailnet**. Node identity, the
AGENTS.md :: tighten :: ACL, the bind decision, and the MagicDNS failure that took down three services:
AGENTS.md :: tighten :: Anything that writes - curl against the dashboard, SDK scripts, manual
AGENTS.md :: tighten :: and the 2026-07-18 production-write incident:
AGENTS.md :: tighten :: [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md).
AGENTS.md :: tighten :: `ARCHIVER_REDIS_URL` is never inherited. A scratch bus is now a scratch
AGENTS.md :: tighten :: # Populate the cannobserv wheelhouse before installing (see Environment & Tooling):
AGENTS.md :: tighten :: uv sync                                      # install deps (resolves co-core from ./.wheelhouse)
AGENTS.md :: tighten :: # Pre-commit hooks (one-time per clone, then runs on each git commit):
AGENTS.md :: tighten :: uv run pre-commit install                    # install the hook
AGENTS.md :: tighten :: uv run pre-commit run --all-files            # manual sweep across the repo
AGENTS.md :: tighten :: # CI mirrors these checks; failing tests/lint locally also fails CI on push/PR.
AGENTS.md :: tighten :: periodic journald line; the `archiver-bus-health` timer (#130) re-runs the
AGENTS.md :: tighten :: query from outside the publisher process, the only surface still reporting
AGENTS.md :: tighten :: when the publisher is down. **Broker-side monitoring is not this repo's** -
AGENTS.md :: tighten :: memory, `XLEN`, last-entry age, `XPENDING`, DLQ depth and disk are
AGENTS.md :: tighten :: CannObserv/broker's (#193 D6). Never on `/health` (unauthenticated, DB-free).
AGENTS.md :: tighten :: See [docs/BUS.md](docs/BUS.md).
AGENTS.md :: tighten :: Per-rule detail throughout this section - what the changelog regex covers and
AGENTS.md :: tighten :: deliberately does not, the `PLC0415` scope and exemptions, the journald
AGENTS.md :: tighten :: contract, and the error envelope's worked examples and `kind` vocabulary:
AGENTS.md :: tighten :: path** - and only then. Path-based, not intent-based; CI and the pre-push guard
AGENTS.md :: tighten :: **Dashboard living docs:** update the doc a change touches in the same commit -
AGENTS.md :: tighten :: PAGES.md (templates, routes), COMPONENTS.md (dashboard JS), UI.md (shared
AGENTS.md :: tighten :: blocker.
AGENTS.md :: tighten :: **Date & Time:** All UTC. ISO 8601: `YYYY-MM-DDTHH:MM:SS.ffffffZ` (timestamps), `YYYY-MM-DD` (dates).
AGENTS.md :: tighten :: hold a submodule - use `.skills/skills-pin`. Each hook, its gates and its log
AGENTS.md :: tighten :: paths: [docs/SKILLS.md](docs/SKILLS.md).
AGENTS.md :: tighten :: - [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) - repository layout tree; co-core acquisition wiring
AGENTS.md :: tighten :: - [docs/API.md](docs/API.md) - every HTTP route, its SDK wrapper, and pagination
AGENTS.md :: tighten :: - [docs/BUS.md](docs/BUS.md) - the outbox producer, the three published streams, and the three consumed
AGENTS.md :: tighten :: - [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) - wheelhouse reproducibility, dev-server internals, full env-var reference
AGENTS.md :: tighten :: - [docs/CONVENTIONS.md](docs/CONVENTIONS.md) - changelog trigger, journald logging contract, error-envelope examples
AGENTS.md :: tighten :: - [docs/SKILLS.md](docs/SKILLS.md) - skill inventory, trigger table, SessionStart hook mechanics
AGENTS.md :: tighten :: - [docs/reference/tailscale.md](docs/reference/tailscale.md) - this node on the tailnet: the ACL, why there is no tailnet-only bind, and the two-names-one-host 

# Re-added after the CR 8-14 fixes: the over-broad prune above removed these
# five alongside the 73 the tool reported spent. Each is still a class-C
# rewrite whose claims carried across (claims_dropped: 0).
AGENTS.md :: tighten :: The exe.dev proxy forwards 3000-9999 and maps the bare hostname to 8000: the
AGENTS.md :: tighten :: `https://co-registrar.exe.xyz:8001/`. Archiver has its own VM (archiver#193);
AGENTS.md :: tighten :: patterns), SCREENS.md (detail screens), STYLE.md (styling). Failing to is a CR
AGENTS.md :: tighten :: Data model identifiers (table names, FastAPI route paths, Redis Stream topics) stay verbatim - never rename casually. The current vocabulary, model ↔ table:
AGENTS.md :: tighten :: Skills live in `skills/` (agentskills.io) and `.claude/skills/` (Claude Code); local overrides in `skills/` shadow vendor submodules in `skills-vendor/`. Cross-
