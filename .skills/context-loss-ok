# Warranted losses (curating-context Phase 6). One judged line each.
#
# The Alpine catalogue index moved from docs/STYLE.md to docs/COMPONENTS.md
# (archiver#178 curation). Every row ended "Full docs in `docs/COMPONENTS.md`."
# - a pointer into the very file the row now lives in. This change is what
# moved the target, so the row arrives with the pointer dropped.
docs/STYLE.md :: retarget :: `sortableChips` | `main.js` | Chip strip for selector/rep-field suggestions
docs/STYLE.md :: retarget :: `repFieldsEditor` | `main.js` | Wrapper for the rep_fields textarea
docs/STYLE.md :: retarget :: `domainNotes` | `main.js` | Edit/view toggle for the notes row
docs/STYLE.md :: retarget :: `registerWizard` | `main.js` | 4-step registration wizard state
docs/STYLE.md :: retarget :: `previewNameDispatch` | `main.js` | One-shot dispatcher: bubbles a `preview-name`
docs/STYLE.md :: retarget :: `urlCheckDispatch` | `main.js` | One-shot dispatcher (#53)

# archiver#181 curation: `## Detail Screen Conventions` was 56% of docs/UI.md,
# so it split out to docs/SCREENS.md. Three lines described UI.md as the
# file holding those conventions and one counted its siblings; this change is
# what moved the target, so each is rewritten in place rather than relocated.
# The section body itself moved verbatim - 200 lines, 0 duplicated.
docs/UI.md :: retarget :: patterns, and the conventions every detail screen follows.
docs/UI.md :: retarget :: The other two parts of this reference, under the same living-doc rule:
docs/UI.md :: retarget :: **The dashboard's shared mechanics: URL map, proxy-header auth, HTMX swap

# archiver#182 curation: four HTMX/Alpine pattern sections left docs/STYLE.md for
# docs/UI.md, whose stated scope is the dashboard's shared mechanics - none of
# the four contained any CSS. Three moved whole. The fourth was a near-duplicate
# of UI.md's own "Inline validation errors (`hx-target-422`)": UI.md had the
# prose, STYLE.md the worked example and the rules. The body merged into that
# canonical section rather than landing beside it as a second copy, so only the
# now-redundant heading has no home.
docs/STYLE.md :: rename :: ### HTMX inline form error pattern

# Same change: the pointer named docs/STYLE.md for a section this run moved to
# docs/UI.md, so it is retargeted in place rather than relocated.
docs/COMPONENTS.md :: retarget :: the reason it exists is `docs/STYLE.md` § **JSON data island pattern**

# The Error-pages body moved from docs/PAGES.md to docs/UI.md (archiver#112 CR
# round 2, finding 6). Its closing pointer named UI.md as an external doc; inside
# UI.md that self-link read as pointing elsewhere, so it was retargeted to the
# intra-doc section reference.
docs/UI.md :: retarget :: [UI.md](UI.md) § Failures are surfaced, not swallowed.

# 2026-09-11 per-doc curation: docs/BUS.md (10714) and docs/PAGES.md (10257)
# were over the 10000 per-doc budget. BUS.md split on its own produce/consume
# line - its four consumer-side sections moved verbatim to docs/BUS_CONSUMERS.md
# - and PAGES.md's Home health-row block moved verbatim to docs/HEALTH_ROW.md.
# Each `retarget` line below was rewritten because this change moved what it
# names: the index entries gain the two new docs; the provenance pointer names
# both bus docs; BUS.md's two in-file pointers aim at the files that now hold
# their targets; and BUS.md's opening, rewritten after the fresh-eyes review,
# describes the producing half it keeps.
AGENTS.md :: retarget :: Central registry + authoring service for the Cannabis Observer information layer.
AGENTS.md :: retarget :: - [docs/BUS.md](docs/BUS.md) - the outbox producer; the three streams published and the three consumed
AGENTS.md :: retarget :: - The dashboard docs - [docs/UI.md](docs/UI.md) shared mechanics and the index to the rest:
docs/BUS.md :: retarget :: Surfaced twice: the dashboard badge (`/dashboard/health/outbox`, PAGES.md) and
docs/BUS.md :: retarget :: own timer. Both are documented under the consumer section below.
docs/BUS.md :: retarget :: Everything Archiver puts on or takes off the Redis bus: the outbox producer and
docs/BUS.md :: retarget :: `content.replicate` - and the three it consumes - `content.revisions`,
docs/BUS.md :: retarget :: `content.artifacts`, `info.watch-status`. HTTP routes and their SDK wrappers
docs/BUS.md :: retarget :: live in [API.md](API.md); this file is the wire side of the same surface.

# Same run, Phase 5 step 1, fixed in place before the move: "one of exactly two"
# consumer roles predates archiver#170. `src/core/changes/artifacts_consumer.py`
# exists and src/api/main.py starts it as `artifacts_consumer_task` - three roles.
docs/BUS.md :: disproven :: Archiver's first consumer role, and one of exactly two - the other is the

# 2026-09-11 AGENTS.md curation (6234 -> 5688 exact). Every entry below is a
# class-C rewrite in place, or class-B material whose destination already owned
# it in more depth - so a verbatim move would have appended near-duplicates, the
# 2026-09-09 round-2 precedent. --claims confirms every atom carried across
# (0 dropped). Where each dropped prose claim lives now:
#   SessionStart Hooks - the hook list and the "both halves" why:
#     docs/SKILLS.md § SessionStart Hooks. The four rules and the test stay.
#   Agent Skills - Linked Projects / .claude/settings.local.json: docs/SKILLS.md.
#   Vocabulary - the 11-pair model<->table map: docs/SCHEMA.md § Entities lists
#     every pair as **Model** (table). The never-rename rule and the InfoSpec
#     prohibition stay.
#   Bus monitoring - rewritten rule-first; "broker-side monitoring is
#     CannObserv/broker's (#193 D6)" stays in the bullet (CR 5 restored it: the
#     monitoring task is where that failure recurs).
#   Infrastructure - "own VM" is Environment & Tooling's; the third node is the
#     network-hop paragraph and docs/reference/tailscale.md.
#   Content-acquisition - the deleted-mirror history: docs/ARCHITECTURE.md.
#   Code Exploration - the health hook's report: docs/SKILLS.md.
#   Environment & Tooling - "dedicated since" -> present tense, same claims.
AGENTS.md :: tighten :: Python ≥3.12, uv, pytest, ruff. **Postgres 16 on archiver's own VM** - dedicated since #193 D5, not the instance shared with watcher and notifier. Three databas
AGENTS.md :: tighten :: SocratiCode is configured here (`.socraticodecontextartifacts.json`); the index is per-host, and the SessionStart health hook reports whether it is built. Its M
AGENTS.md :: tighten :: Fetch, extract, and the content fingerprint come from **co-core**; the former
AGENTS.md :: tighten :: `src/core/{fetchers,extractors,simhash,extraction_defaults}` mirror is deleted. Wiring: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).
AGENTS.md :: tighten :: Archiver has its own VM (archiver#193); the broker is on a third node
AGENTS.md :: tighten :: (CannObserv/broker#1).
AGENTS.md :: tighten :: - Bus monitoring: outbox stats (archiver#112) on the dashboard badge + a
AGENTS.md :: tighten :: periodic journald line; the `archiver-bus-health` timer (#130) re-runs the query from
AGENTS.md :: tighten :: outside the publisher process, the only surface still reporting when the
AGENTS.md :: tighten :: publisher is down. **Broker-side monitoring is not this repo's** - it is
AGENTS.md :: tighten :: CannObserv/broker's, on the broker's node (#193 D6). Never on `/health`
AGENTS.md :: tighten :: (unauthenticated, DB-free). See [docs/BUS.md](docs/BUS.md).
AGENTS.md :: tighten :: Data model identifiers (table names, FastAPI route paths, Redis Stream topics) stay verbatim - never rename casually. Model ↔ table:
AGENTS.md :: tighten :: `InfoItem` ↔ `info_items` · `InfoSource` ↔ `info_sources` · `SourceRevision` ↔
AGENTS.md :: tighten :: `source_revisions` · `InfoItemSource` ↔ `info_item_sources` · `RepSpec` ↔
AGENTS.md :: tighten :: `rep_specs` · `InfoItemRepSpec` ↔ `info_item_rep_specs` · `ChangesOutboxRow` ↔
AGENTS.md :: tighten :: `changes_outbox` · `RevokedInfoItem` ↔ `revoked_info_items` · `WatchStatus` ↔
AGENTS.md :: tighten :: `watch_status` · `ReplicationCommand` ↔ `replication_commands` ·
AGENTS.md :: tighten :: `BusTailCursor` ↔ `bus_tail_cursors`
AGENTS.md :: tighten :: What each one is, plus its contracts and invariants:
AGENTS.md :: tighten :: [docs/SCHEMA.md](docs/SCHEMA.md) - it documents every one of them. The Phase
AGENTS.md :: tighten :: 1-3a `InfoSpec` model is retired - no new `info_spec*` references.
AGENTS.md :: tighten :: Skills live in `skills/` (agentskills.io) and `.claude/skills/` (Claude Code); overrides in `skills/` shadow vendor submodules in `skills-vendor/`. Cross-projec
AGENTS.md :: tighten :: `.claude/settings.json` wires three hooks: the SocratiCode prefetch reminder,
AGENTS.md :: tighten :: the once-per-day SocratiCode health check, and the once-per-day `skills-vendor/`
AGENTS.md :: tighten :: refresh. Both halves are load-bearing - a script `settings.json` does not name
AGENTS.md :: tighten :: never runs and looks identical to one that works
AGENTS.md :: tighten :: (`tests/scripts/test_claude_hooks_registered.py` fails on the missing half).
AGENTS.md :: tighten :: All three scripts are symlinks into `skills-vendor/`: never re-copy one, never
AGENTS.md :: tighten :: turn the committed `.skills/doctor.sh` into one, and never un-wire a hook to
AGENTS.md :: tighten :: hold a submodule - use `.skills/skills-pin`. Each hook and its logs:
AGENTS.md :: tighten :: [docs/SKILLS.md](docs/SKILLS.md).
