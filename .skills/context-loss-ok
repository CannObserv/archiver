# Warranted losses (curating-context Phase 6). One judged line each.
#
# The Alpine catalogue index moved from docs/STYLE.md to docs/COMPONENTS.md
# (archiver#178 curation). Every row ended "Full docs in `docs/COMPONENTS.md`."
# - a pointer into the very file the row now lives in. This change is what
# moved the target, so the row arrives with the pointer dropped.
docs/STYLE.md :: retarget :: `sortableChips` | `main.js` | Chip strip for selector/rep-field suggestions
docs/STYLE.md :: retarget :: `repFieldsEditor` | `main.js` | Wrapper for the rep_fields textarea
docs/STYLE.md :: retarget :: `domainNotes` | `main.js` | Edit/view toggle for the notes row
docs/STYLE.md :: retarget :: `registerWizard` | `main.js` | 4-step registration wizard state
docs/STYLE.md :: retarget :: `previewNameDispatch` | `main.js` | One-shot dispatcher: bubbles a `preview-name`
docs/STYLE.md :: retarget :: `urlCheckDispatch` | `main.js` | One-shot dispatcher (#53)

# archiver#181 curation: `## Detail Screen Conventions` was 56% of docs/UI.md,
# so it split out to docs/SCREENS.md. Three lines described UI.md as the
# file holding those conventions and one counted its siblings; this change is
# what moved the target, so each is rewritten in place rather than relocated.
# The section body itself moved verbatim - 200 lines, 0 duplicated.
docs/UI.md :: retarget :: patterns, and the conventions every detail screen follows.
docs/UI.md :: retarget :: The other two parts of this reference, under the same living-doc rule:
docs/UI.md :: retarget :: **The dashboard's shared mechanics: URL map, proxy-header auth, HTMX swap

# archiver#181 CR round 2, finding 9: three PAGES.md detail entries re-derived
# the canonical `.entity-card` header anatomy - eyebrow, the `<h1
# class="entity-card__title" id="…" tabindex="-1">`, the focus-target reason -
# once per screen. That anatomy acquired a canonical home in this same change
# (docs/SCREENS.md § Header), so each entry now points there and keeps only what
# is true of its own screen: the eyebrow text, the title value, the heading id,
# and the issue that adopted it. Nothing screen-specific was dropped.
docs/PAGES.md :: retarget :: `.eyebrow` "Domain" kicker
docs/PAGES.md :: retarget :: `.eyebrow` "Information Source" → `<h1 class="entity-card__title" id="info-source-heading"
docs/PAGES.md :: retarget :: `.eyebrow` "Replication Specification" → `<h1 class="entity-card__title" id="rep-spec-heading"

# archiver#182 curation: four HTMX/Alpine pattern sections left docs/STYLE.md for
# docs/UI.md, whose stated scope is the dashboard's shared mechanics - none of
# the four contained any CSS. Three moved whole. The fourth was a near-duplicate
# of UI.md's own "Inline validation errors (`hx-target-422`)": UI.md had the
# prose, STYLE.md the worked example and the rules. The body merged into that
# canonical section rather than landing beside it as a second copy, so only the
# now-redundant heading has no home.
docs/STYLE.md :: rename :: ### HTMX inline form error pattern

# Same change: the pointer named docs/STYLE.md for a section this run moved to
# docs/UI.md, so it is retargeted in place rather than relocated.
docs/COMPONENTS.md :: retarget :: the reason it exists is `docs/STYLE.md` § **JSON data island pattern**

# The Error-pages body moved from docs/PAGES.md to docs/UI.md (archiver#112 CR
# round 2, finding 6). Its closing pointer named UI.md as an external doc; inside
# UI.md that self-link read as pointing elsewhere, so it was retargeted to the
# intra-doc section reference.
docs/UI.md :: retarget :: [UI.md](UI.md) § Failures are surfaced, not swallowed.

# archiver#147 CR finding 7. The Conventions section carried four separate
# trailing pointers at docs/CONVENTIONS.md; they are one pointer at the section
# head now, which names every target the four named between them (regex scope,
# PLC0415 exemptions, the journald contract, the error envelope's kind
# vocabulary). `duplicate` is the warrant: the surviving head pointer carries
# the destination and every target the four named, so the content is elsewhere
# in the surface rather than dropped.
AGENTS.md :: duplicate :: What the regex covers and what it deliberately does not:
AGENTS.md :: duplicate :: patterns), SCREENS.md (detail screens), STYLE.md (styling). Failing to is a CR blocker; per-trigger detail in
AGENTS.md :: duplicate :: a journald consumer must tolerate that ([docs/CONVENTIONS.md](docs/CONVENTIONS.md)).
AGENTS.md :: duplicate :: (archiver#97). Scope and exemptions: [docs/CONVENTIONS.md](docs/CONVENTIONS.md).
AGENTS.md :: duplicate :: `except X as e:`. Shape, worked examples, and the `kind` vocabulary:
