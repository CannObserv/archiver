# Judged seam acknowledgements — see check-seams.sh --help.
# One entry per judged line.

# The three bus headings moved verbatim out of API.md. Their issue numbers were
# already in the slug before the move; renaming them now would rewrite relocated
# content for no reader benefit, and nothing links to those anchors.
docs/BUS.md :: Change-bus consumer - `content.revisions` (archiver#139)
docs/BUS.md :: Change-bus consumer - `content.artifacts` (archiver#170)
docs/BUS.md :: Change-bus tail - `info.watch-status` (archiver#151)

# Predates this run and is unrelated to it: a retirement note whose issue
# numbers are the record, not a pointer at moved content.
docs/DEPLOYMENT.md :: retired (archiver#150

# doc-check.sh:37 is the one entry here that is NOT accurate: DOC_SECTIONS says
# AGENTS.md carries a route table and a skill inventory, and neither is true -
# they live in docs/API.md and docs/SKILLS.md. Acknowledged rather than fixed
# (archiver#185 CR round 1). The array is advisory output, printed verbatim when
# the check exits 1; SENSITIVE_PATHS is what drives the matching and it is
# correct. The file is a symlink into skills-vendor, so correcting one hint line
# means forking a 117-line maintained script - a worse trade than the
# imprecision. The real fix is upstream: make DOC_SECTIONS sourceable from a
# project file, the way .skills/skills-pin and .skills/context-budget already
# are.
skills/shipping-work-python-fastapi/scripts/doc-check.sh :: project structure, conventions, skill inventory, route table

# archiver#185 curation. Fifteen docs carry a back-reference naming what
# AGENTS.md still holds - the auth rule, the never-rename rule, the safety-rule
# split, the Code Exploration Policy, the living-doc enforcement banners, the
# Detail Docs index. This run moved nothing out of the policy file (it deleted
# three restatements of a rule stated canonically there and one duplicated
# command), so every one of them still tells the truth.
docs/API.md :: `AGENTS.md` carries only the auth rule and a pointer here
docs/ARCHITECTURE.md :: Repository layout and the content-acquisition data flow
docs/ARCHITECTURE.md :: in [AGENTS.md](../AGENTS.md); the `docs/` entry in the tree below mirrors it
docs/ARCHITECTURE.md :: Indexed by the Detail Docs section of AGENTS.md
docs/ARCHITECTURE.md :: mirror discipline") refer to sections of [AGENTS.md](../AGENTS.md)
docs/COMPONENTS.md :: **AGENTS.md enforcement:** update this file in the same commit as any change
docs/CONVENTIONS.md :: The reasoning and worked examples behind the rules stated in `AGENTS.md`
docs/DEPLOYMENT.md :: `AGENTS.md` keeps the safety rules; the reference lives here
docs/PAGES.md :: **AGENTS.md enforcement:** update this file in the same commit as any Jinja2
docs/PAGES.md :: SDK, and AGENTS.md's no-outbound-HTTP rule left nothing to ping
docs/SCHEMA.md :: see the never-rename rule in `AGENTS.md`
docs/SKILLS.md :: `AGENTS.md` keeps only the negative rule and points here
docs/SKILLS.md :: see **Code Exploration Policy** in `AGENTS.md`
docs/STYLE.md :: **AGENTS.md enforcement:** This file must be updated in the same commit
docs/UI.md :: **AGENTS.md enforcement:** a Jinja2 template change, a new or changed

# Same run, source side. The heading rename widened the sweep to tracked source;
# every hit names the policy file generically - the write guard and the refresh
# hook that measure it, the SocratiCode manifest that indexes it, and five
# comments citing a convention it still states. Two historical notes say
# "CLAUDE.md used to ...", which is a record rather than a pointer. None refers
# to the renamed heading, and this run moved nothing out of the file.
.claude/hooks/context-budget-guard.sh :: the next fortnight of edits puts it back
.claude/hooks/context-budget-guard.sh :: the policy file: AGENTS.md or CLAUDE.md at the repo root
.claude/hooks/context-budget-guard.sh :: CLAUDE.md is followed to its target, which is the cohort norm
.claude/hooks/context-budget-guard.sh :: AGENTS.md|CLAUDE.md) KIND="policy"
.claude/hooks/skills-submodule-update.sh :: The remedy is log-and-skip, not the exit-2 of AGENTS.md's gate
.socraticodecontextartifacts.json :: "path": "./AGENTS.md"
pyproject.toml :: PLC0415 enforces the AGENTS.md "no inline module imports" convention
scripts/dev_server.sh :: CLAUDE.md used to document a raw uvicorn recipe for the dev server
skills/shipping-work-python-fastapi/scripts/doc-check.sh :: or structure is referenced from project docs
skills/shipping-work-python-fastapi/scripts/doc-check.sh :: "AGENTS.md"
src/core/db_safety.py :: Suffixes that mark a database as disposable
tests/api/test_lifespan_bus_wiring.py :: safety control (`AGENTS.md`, "Four variables carry safety rules")
tests/core/test_db_safety.py :: it matches the convention AGENTS.md already documents
tests/scripts/test_dev_server.py :: The dev server (port 8021) previously had no launch script

# archiver#156 added a fourth provenance-slug bus heading, same judgement as the
# three above: the issue number was in the slug from birth, nothing links the anchor.
docs/BUS.md :: Consumer names are a monitoring contract (archiver#156)

# 2026-08-27 PAGES.md curation (archiver#112 branch): action-route rationale moved
# to INFO_ITEM_DETAIL.md § Action-route contracts, Error pages body moved to UI.md.
# Every back-reference below says PAGES.md keeps the inventory/pointer line - which
# this run preserved - or links PAGES.md as the route inventory, which it remains.
docs/ARCHITECTURE.md :: UI.md + SCREENS.md + PAGES.md +
docs/BUS.md :: Surfaced twice: the dashboard badge (`/dashboard/health/outbox`, PAGES.md) and
docs/COMPONENTS.md :: Where each component is used is in [PAGES.md](PAGES.md); the HTMX patterns they
docs/COMPONENTS.md :: Multi-step Information Item registration wizard. Manages step navigation and form state across the 4-step URL
docs/CONVENTIONS.md :: - `docs/PAGES.md` - required for any change to a Jinja2 template in
docs/CONVENTIONS.md :: action-route contracts that moved there in archiver#176. PAGES.md keeps the
docs/CONVENTIONS.md :: convention updates PAGES.md alone.
docs/CONVENTIONS.md :: A template change that composes only existing CSS classes needs PAGES.md alone.
docs/INFO_ITEM_DETAIL.md :: templates, and their swap targets.** Split out of [PAGES.md](PAGES.md) when that
docs/INFO_ITEM_DETAIL.md :: it, and nobody working on Domains or RepSpecs needs to load it. PAGES.md keeps
docs/INFO_ITEM_DETAIL.md :: Moved here from [PAGES.md](PAGES.md), which keeps the inventory line for each of
docs/INFO_ITEM_DETAIL.md :: that announce and let Watcher converge; the route entries in [PAGES.md](PAGES.md) name only what each adds.
docs/INFO_ITEM_DETAIL.md :: **Every outcome is a 200, and every outcome flashes** via `HX-Trigger: showFlash`
docs/SCREENS.md :: Sibling docs: [UI.md](UI.md) shared mechanics - [PAGES.md](PAGES.md)
docs/SCREENS.md :: The screens themselves are inventoried in [PAGES.md](PAGES.md).
docs/SCREENS.md :: [PAGES.md](PAGES.md)) - three rules hold, and they generalise to every such
docs/UI.md :: - [PAGES.md](PAGES.md) - the per-page route inventory: what each screen renders
docs/UI.md :: > in the same commit - usually PAGES.md or COMPONENTS.md, and this file when it
docs/UI.md :: The gate is universal, so the route entries in [PAGES.md](PAGES.md) do not repeat
docs/UI.md :: `/dashboard` path; [PAGES.md](PAGES.md) keeps the pointer entry.
