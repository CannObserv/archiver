# Judged seam acknowledgements — see check-seams.sh --help.
# One entry per judged line.

# The three bus headings moved verbatim out of API.md. Their issue numbers were
# already in the slug before the move; renaming them now would rewrite relocated
# content for no reader benefit, and nothing links to those anchors.
docs/BUS.md :: Change-bus consumer — `content.revisions` (archiver#139)
docs/BUS.md :: Change-bus consumer — `content.artifacts` (archiver#170)
docs/BUS.md :: Change-bus tail — `info.watch-status` (archiver#151)

# archiver#178 curation: the Alpine catalogue index moved from STYLE.md to
# COMPONENTS.md. These pointers name what STYLE.md still holds - tokens,
# component classes, the JSON data island pattern, dark-mode theming - none of
# which moved, so each still tells the truth.
docs/ARCHITECTURE.md :: COMPONENTS.md + STYLE.md
docs/COMPONENTS.md :: the index into them; `docs/STYLE.md` carries the CSS classes they toggle.
docs/COMPONENTS.md :: the reason it exists is `docs/STYLE.md` § **JSON data island pattern**
docs/CONVENTIONS.md :: required when the change introduces or alters *styling*
docs/INFO_ITEM_DETAIL.md :: Alpine catalogue · [STYLE.md](STYLE.md) tokens and component classes.
docs/INFO_ITEM_DETAIL.md :: **Every outcome is a 200, and every outcome flashes**

# Predates this run and is unrelated to it: a retirement note whose issue
# numbers are the record, not a pointer at moved content.
docs/DEPLOYMENT.md :: retired (archiver#150

# Rewritten in this run to name COMPONENTS.md as the catalogue for non-Alpine
# modules; the tail still points flash.js and dark-mode.js at the docs that
# carry their patterns, and neither pattern moved.
docs/CONVENTIONS.md :: ("Flash messages") and `docs/STYLE.md`, `dark-mode.js` in `docs/STYLE.md`.

# archiver#181 curation: SCREENS.md split out of UI.md, so its own two
# back-pointers at UI.md are the split describing itself. Nothing they point at
# moved - UI.md still holds the shared mechanics they name.
docs/SCREENS.md :: Split out of [UI.md](UI.md) when that file passed its context
docs/SCREENS.md :: Sibling docs: [UI.md](UI.md) shared mechanics
